LOG_FORMAT=pretty

DB_HOST=localhost
DB_PORT=5433
DB_NAME=postgres
DB_SSL=false

# App DB user credentials
APP_DB_USER=app_user
APP_DB_PASSWORD=app_password

# Auth DB user credentials
AUTH_DB_USER=auth_user
AUTH_DB_PASSWORD=auth_password

# openssl rand -base64 24 | tr -dc 'a-zA-Z0-9'
BETTER_AUTH_SECRET=

# OpenID Connect provider credentials
KEYCLOAK_CLIENT_ID=fullstack-react-template
KEYCLOAK_CLIENT_SECRET=s3cr3t
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=identity-provider
